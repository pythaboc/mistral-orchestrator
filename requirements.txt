# SDK officiel Mistral (chat.complete, agents, conversations, websearch)
mistralai>=2.0.0

# Gestion des variables d'environnement
python-dotenv>=1.0.0

# Recherche web de fallback (si websearch natif Mistral indisponible)
ddgs>=9.0.0

# Interface web Flask (pour le mode --web)
flask>=3.0.0

# Monitoring Prometheus - optionnel
prometheus-client>=0.20.0
